@model lab7.Models.IntegrationModel

@{
    ViewData["Title"] = "Integration Result";
}

<h2>Integration Result</h2>

<p>
    The integral of <strong>Log10(x) * x + 2 * x + 1</strong> from <strong>@Model.LowerLimit</strong> to <strong>@Model.UpperLimit</strong>
    using <strong>@Model.SubIntervals</strong> subintervals is:
</p>

<h3><strong>@Model.Result</strong></h3>

<a asp-action="Index" class="btn btn-secondary mt-3">Back to Calculator</a>
